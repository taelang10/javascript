; 복습 내용

; 1. 변수 선언 방법
; var, let, const 차이
; 공통점 : var, let는 박스가 열려있어서 값을 넣다 뺏다 가능
; (변수)

; const는 한번 선언 해버리면 박스를 닫아버림
; (상수)

; 2. 내장함수 사용방법 및 아규먼트
; 3. 함수 제작 방법 및 파라미터
; 4. 배열과 객체 차이



;  1. 1++ ->
;  -> 2  (다음줄에 더해 진다)

;  2. ++1 -> 2

;  a='1'
;  a!=1

;  a!=1 true  (1인지 아닌지만 본다)
;  a!==1 false (타입까지 봐준다)

;  a === 1 (=하나더 들어간다면 타입까지 들어간다고 생각하면 된다)


